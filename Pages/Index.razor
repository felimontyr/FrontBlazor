@page "/"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Redirige siempre al login al entrar por "/"
        Navigation.NavigateTo("/login", true);
    }
}
